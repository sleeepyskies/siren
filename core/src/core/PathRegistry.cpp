#include "PathRegistry.hpp"

namespace siren::core
{

namespace fs = std::filesystem;

PathRegistry::PathRegistry()
{
    // determine Project Root, where the .exe was called from
    m_projectRoot = std::

    // determine Engine Root, where the .exe is located
}

} // namespace siren::core