#pragma once

namespace siren::core
{

/**
 * @brief Enum listing various errors that can happen in siren.
 */
enum class ErrorCode {
    NONE = 0,
    // TODO: add some more lol, maybe replace for functions returning bool/void?
    UNKNOWN,
};

} // namespace siren::core
