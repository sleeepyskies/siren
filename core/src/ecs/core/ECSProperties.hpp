#pragma once

// TODO: this should probabaly be put into a Properties config struct we pass to scene on creation

/// @brief The max amount of components a Scene can handle.
constexpr int MAX_COMPONENTS = 32;
